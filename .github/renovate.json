{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "prHourlyLimit": 30,
  "prConcurrentLimit": 30,
  "dependencyDashboard": false,
  "extends": [
    "config:recommended",
    ":disableDependencyDashboard",
    "config:recommended",
    ":disableDependencyDashboard",
    "config:recommended",
    ":disableDependencyDashboard",
    "config:recommended",
    ":disableDependencyDashboard",
    "config:recommended",
    ":disableDependencyDashboard",
    "config:recommended",
    ":disableDependencyDashboard",
    "config:recommended",
    ":disableDependencyDashboard",
    "config:recommended",
    ":disableDependencyDashboard"
  ],
  "forkProcessing": "enabled",
  "ignorePaths": ["vendor/**"],
  "enabledManagers": ["custom.regex"],
  "recreateWhen": "always",
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "bundle/manifests/gpu-operator-certified.clusterserviceversion.yaml"
      ],
      "matchStrings": [
        "[-\\s]*image: (?<depName>.*?)(?::(?<tag>.*?))?@(?<currentValue>sha256:[a-f0-9]{64})"
      ],
     "datasourceTemplate": "docker",
      "packageNameTemplate": "nvcr.io/nvidia/driver",
      "versioningTemplate": "docker",
      "extractVersionTemplate": "digest"
    }
  ],
  "packageRules": [
    {
      "matchDatasources": ["docker"],
      "matchPackageNames": ["nvcr.io/nvidia/driver"],
      "matchFileNames": ["driver-image"],
      "groupName": "driver-image"
    },
    {
      "matchDatasources": ["docker"],
      "matchPackageNames": ["nvcr.io/nvidia/driver"],
      "matchFileNames": ["driver-image-550"],
      "groupName": "driver-image-550"
    },
    {
      "matchDatasources": ["docker"],
      "matchPackageNames": ["nvcr.io/nvidia/driver"],
      "matchFileNames": ["driver-image-535"],
      "groupName": "driver-image-535"
    }
  ]
}