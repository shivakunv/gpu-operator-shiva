{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",

  "extends": ["config:recommended", ":disableDependencyDashboard"],
  "enabledManagers": ["custom.regex"],
  "forkProcessing": "enabled",
  "recreateWhen": "always",
  "pruneStaleBranches": true,

  "customManagers": [

    /* -------------------------------
       1. VALUES.YAML MANAGER
       -------------------------------- */
    {
      "customType": "regex",
      "managerFilePatterns": [
        "deployments/gpu-operator/values.yaml"
      ],
      "matchStrings": [
        "repository:\\s*(?<repo>[^\\s]+)\\s*\\n\\s*image:\\s*(?<image>[^\\s]+)\\s*\\n\\s*version:\\s*(?<currentValue>[^\\s]+)"
      ],
      "depNameTemplate": "{{repo}}/{{image}}",
      "datasourceTemplate": "docker",
      "extractVersionTemplate": "{{currentValue}}"
    },

    /* -------------------------------
       2. CSV MANAGER (FOLLOW VALUES.YAML)
       -------------------------------- */
    {
      "customType": "regex",
      "managerFilePatterns": [
        "bundle/manifests/gpu-operator-certified.clusterserviceversion.yaml"
      ],
      "matchStrings": [
        "image:\\s*(?<depName>[^:@]+):(?<currentValue>[^@]+)@(?<currentDigest>sha256:[a-f0-9]{64})"
      ],

      "depNameTemplate": "{{depName}}",
      "datasourceTemplate": "docker",

      /* Extract version and digest via regex groups */
      "extractVersionTemplate": "{{currentValue}}"
    }
  ],

  /* -------------------------------
     PACKAGE RULES
     -------------------------------- */
  "packageRules": [
    {
      /* REQUIRED: add a selector so the rule is valid */
      "matchDatasources": ["docker"],
      "allowedVersions": "^\\d+\\.\\d+\\.\\d+$"
    },
    {
      /* Group VALUES + CSV updates in the same PR */
      "matchPaths": [
        "deployments/gpu-operator/values.yaml",
        "bundle/manifests/gpu-operator-certified.clusterserviceversion.yaml"
      ],
      "groupName": "{{depName}}",
      "separateMajorMinor": false,
      "separateMinorPatch": false
    },
    {
      /* enable digest pinning */
      "matchDatasources": ["docker"],
      "pinDigests": true
    }
  ]
}

