{
  "enabledManagers": ["regex"],
  "regexManagers": [
    {
      "fileMatch": ["^deployments/gpu/.*\\.ya?ml$"],
      "matchStrings": [
        "image:\\s*(?<depName>[\\w./-]+):(?<currentValue>[\\w.-]+)"
      ],
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    }
  ],
  "schedule": ["before 4am every day"]
}
